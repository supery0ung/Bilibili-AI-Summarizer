视频标题：{title}
UP主：{author}

以下是视频开头的转录文本。请根据内容分析并输出说话人的身份映射，仅输出 JSON 格式。

### 关键要求（必须严格遵守）：
1. **JSON 键名必须是原始标签**：键名必须是文本中出现的 `"[说话人 A]"`、`"[说话人 B]"` 等字面值，**不得修改键名**。
2. **JSON 映射值**：
   - 如果说话人是 UP主（{author}），映射值为 **"主持人"**。
   - 如果识别出真实姓名（参考"我是xxx"等表述），映射值为其**真实姓名**。
   - 无法确定姓名或身份时，映射值为 **null**。
3. **单人视频处理**：如果分析判断整个视频只有一个人在说话（即使 ASR 标记了多个 ID），请**输出空 JSON `{}`**。只有确定有多个不同身份的人对话时才输出映射。
4. **输出示例**：
   - 多人对话：{{"[说话人 A]": "主持人", "[说话人 B]": "路人甲", "[说话人 C]": null}}
   - 单人视频：{{}}

### 识别规则：
1. **UP主 = "主持人"**：在对话类视频中，UP主即主持人。
2. **真实姓名提取**：只提取文本中**明确出现过**的名字（如"我是张三"、"欢迎李四"）。
3. **严禁猜测或编造**：如果文本中没有明确提到某人的名字，映射值**必须为 null**。绝对不要根据声音、语气、话题等推测名字。宁可输出 null 也不要猜。
4. **严禁职业泛称**：不要使用"专家"、"CEO"、"嘉宾"等描述。

/no_think

文本：
{text}
